# Hermes Korean Stock YTD Chart 계획서 분석 및 리뷰 보고서 (Review & Opinion)

본 보고서는 `HERMES_KOREAN_STOCK_YTD_CHART_TEST_PLAN.md`에 제시된 설계와 테스트 방안을 검증하고, Antigravity, Codex, Claude Code 간의 협업 맥락과 에이전트 자율성(자율 회복, 자원 거버넌스, 분석 안정성) 관점의 추가 개선점을 제안하기 위해 작성되었습니다.

---

## 1. 계획서 핵심 진단 및 긍정적 측면

### 1) 날카로운 인텐트 오분류(Intent Misclassification) 분석
* **진단**: 사용자가 새로운 동적 차트 계산 및 시각화를 요구했음에도, 기존의 `isSendLatestImagesRequest` 정규식이 단순 텍스트 매칭(`차트`, `텔레그램`)에 의해 넓은 그물망에 걸려 `send-latest-images`로 라우팅된 원인을 정확히 짚어냈습니다.
* **평가**: 문제의 심각성을 `P0`로 분류하고, 전용 인텐트 `isKoreanStockYtdRankingChartRequest`를 정규식 상위에 배치함과 동시에 기존 이미지 재전송 정규식에 부정형 체크(Negative Check: `가장 많이 상승`, `3년치`, `수익률` 등)를 강화하는 구조는 인텐트 간 충돌을 방지하는 실질적이고 즉각적인 해결책입니다.

### 2) 자원 거버넌스(REST Budget Governance)의 준수
* **진단**: 봇이 로컬 시스템의 수천 개 종목에 대해 개별 루프를 돌며 API 조회를 시도할 때 발생할 수 있는 밴(IP Ban) 및 타임아웃 문제를 예방하기 위해, **pykrx의 전 종목 일괄 스냅샷(Snapshot) 수집 방식**을 대안으로 제시했습니다.
* **평가**: 2026년 첫 거래일 스냅샷 1회, 당일 거래일 스냅샷 1회로 총 2회만 네트워크 요청을 보내 YTD를 연산하고, 최종 10개 종목에 대해서만 개별 3개년 일봉을 받아오는 방식(총 12회 API 호출)은 Codex 및 Claude Code가 이전 대화에서 강조한 REST 호출 최소화 가이드라인에 완벽히 부합합니다.

---

## 2. 에이전트 협업 및 자율적 분석 관점의 추가 개선 제안

Antigravity 에이전트로서, 계획서에 언급되지 않았으나 실제 구현 시 발생할 수 있는 잠재적 실패 요인(Edge Cases)과 자율 회복 방안을 다음과 같이 보완 제안합니다.

### 1) 신규 상장, 거래 정지 및 상장 폐지 종목의 예외 처리 (Data Cleansing)
* **문제점**: 2026년 1월 1일 이후 신규 상장된 종목은 첫 거래일 스냅샷에서 가격 데이터를 찾을 수 없거나 결측치(NaN)로 반환됩니다. 또한 2026년 내내 거래 정지 상태였던 종목은 비정상적으로 높은 등락률로 계산되거나 분모가 0이 되는 연산 오류를 유발할 수 있습니다.
* **개선안**: YTD 계산식 실행 전 데이터 클렌징 단계를 명문화해야 합니다.
  * 계산 대상에서 거래 대금이 극도로 낮거나(예: 거래대금 0원), 2026년 첫 거래일 종가가 조회되지 않는 신규 상장 종목은 연산 필터에서 제외(Filter Out)합니다.
  * 분모가 0인 경우 및 데이터 결측치는 Safe Divide(안전한 나눗셈) 로직을 통해 0%로 처리합니다.

### 2) 한국어 폰트 깨짐(Mojibake/Squircle) 방지 및 차트 템플릿 표준화
* **문제점**: 윈도우 환경에서 `matplotlib` 또는 `mplfinance`를 활용해 차트를 렌더링할 때, 기본 폰트에 한글 글꼴이 매핑되어 있지 않으면 차트 제목이나 종목명이 네모(`ㅁ`) 형태로 깨지는 모지바케 현상이 발생합니다.
* **개선안**: 차트 생성 스크립트에 한글 폰트를 명시적으로 로드하는 코드를 반드시 포함해야 합니다.
  ```python
  import matplotlib.pyplot as plt
  plt.rcParams['font.family'] = 'Malgun Gothic' # Windows 표준 한글 폰트 지정
  plt.rcParams['axes.unicode_minus'] = False    # 마이너스 기호 깨짐 방지
  ```
  추가로, 종목 이름 뒤에 단축 코드(예: `삼성전자 (005930)`)를 병기하여 전송 가독성을 극대화합니다.

### 3) 헬퍼 스크립트 분리 및 Node.js-Python 인터페이스 단일화
* **문제점**: Node.js 파일(`telegram-flow-news-bot.mjs`) 내에 데이터 정렬, 랭킹 연산, `matplotlib` 시각화 호출 등 방대한 비즈니스 로직을 자바스크립트로 길게 작성하게 되면 봇 코드의 복잡도가 극도로 가중됩니다.
* **개선안**: 이미 구축되어 있는 SQLite 연동 파이썬 헬퍼 아키텍처를 참고하여, YTD 분석 전용 파이썬 스크립트(`krx_ytd_helper.py`)를 독립 실행 파일로 격리하는 구조가 권장됩니다.
  * Node.js 봇은 사용자의 인텐트가 분류되면 단순히 `python krx_ytd_helper.py --top 10 --output-dir <경로>` 형태의 CLI 파워쉘 프로세스를 자식(spawn)으로 실행시킵니다.
  * 파이썬 스크립트는 차트 드로잉 및 결과 요약 JSON을 지정 폴더에 생성하고 종료됩니다.
  * Node.js 봇은 반환된 JSON과 이미지들을 텔레그램 API를 통해 사용자에게 단순 전송하는 역할만 전담하여 아키텍처를 대폭 단순화시킵니다.

### 4) 자율적 데이터 소스 폴백(Self-Recovery) 체계 마련
* **문제점**: pykrx는 공공 데이터 포털이나 KRX 웹사이트에서 데이터를 크롤링해 오므로, 일시적으로 KRX의 웹 차단이나 서버 지연(503 Service Unavailable)이 발생할 때 데이터 수집에 완전히 실패하여 감시견(Watchdog)에 잡히게 됩니다.
* **개선안**: 주 소스(pykrx)가 실패할 경우를 대비하여 **보조 데이터 소스로의 1회 자율 폴백(Fail-over)** 메커니즘을 헬퍼 스크립트 내부에 탑재합니다.
  * pykrx 실패 시 -> `FinanceDataReader` 또는 `naver finance scraper`를 호출하여 2026년 연초 종가와 현재가 스냅샷을 긴급 복구 수집하는 2안(Plan B)을 명문화합니다.

---

## 3. 종합 의견 및 평Verdict

본 계획서는 기존의 심각한 인텐트 라우팅 버그(P0)를 깔끔하게 해소함과 동시에, 네트워크 트래픽 부하를 최소화하려는 훌륭한 시스템 거버넌스를 갖추고 있습니다.

다만, 실제 서비스 수준(Production Grade)의 신뢰성을 담보하기 위해 **"데이터 결측 처리"**, **"차트 한글 폰트 깨짐 예외 방지"**, **"Python CLI 헬퍼 격리화"** 및 **"데이터 소스 자율 폴백 체계"**가 구현 설계서에 추가 보완되어야 합니다. 이 보완책들이 결합된다면, Antigravity-Codex-Claude Code 에이전트 연합군이 제시하는 최상의 시너지 성과물이 도출될 것으로 판단됩니다.

---
*본 검토 리포트 문서는 사용자 요청에 따라 **UTF-8** 형식으로 인코딩하여 저장되었습니다.*
*저장 경로: `C:\Users\amd\hermes\HERMES_KOREAN_STOCK_YTD_CHART_REVIEW.md`*
